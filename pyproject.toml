[project]
name = "meridian-tool-lint-scope"
version = "0.1.0"
description = "Package a Python local lab for lint analysis with transition tables, invalid-transition tests, and documented operating limits."
requires-python = ">=3.11"
